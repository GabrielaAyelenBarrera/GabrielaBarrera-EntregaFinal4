.notas.txt
<!-- notas.txt
    *gitignore: cree una carpeta que no sea leida por git.
    *git init: solo se realiza una sola vez, para iniciar el proyecto.
    *git status: sirve para saber si los cambios generados en las carpetas fueron guardados.
    *gid add: sirve para agregar los cambios.
    *git commit -m"Estado inicial del proyecto": es la primera "instantanea" del proyecto(Entre comillas va un titulo especifico p/saber q cambios se realizaron)
    *git log: sirve para ver cuantas versiones tengo del proyecto.
    *git log --oneline: devuelve un listado pero con menos detalles, pero con la cantidad de intantaneas creadas.
    *git checkuot y el ID :puede trabajar en otro proyecto, cambia el ID principal o puedo volver a una rama anterior(tener una rama con todos los cambios realizados)
    *git branch: me dice en que rama estoy trabajando.
    *git branch + nombre : sirve p/crear una nueva rama.
    *git merge + nombre de la rama: trae los cambios que realice en una rama a la rama principal para que se vean los cambios.
    *Si cierro la terminal se borra el historial.
    *Github : mail gabrielaayelenbarrera1145@ pass 011674541...
    *git remote: 
    *git remote -v:
    *git branch -M main: modifica el nombre de la rama Master a Main.
    *git push -u origin main: 
    *gitignore.io : 









-->
